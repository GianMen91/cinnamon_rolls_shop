// Import the Flutter material library
import 'package:flutter/material.dart';

// Define color constants for text and background
const lightTextColor = Color(0xFFBDC2FF);
const darkTextColor = Color(0xFF392C50);
const backgroundColor = Color(0xFFF4F1EA);
const menuBackgroundColor = Color(0xFFF4F1EA);

// Define a constant for default padding
const defaultPadding = 20.0;
